# package.json exists only to pin the spec tooling (redocly, openapi-typescript, js-yaml) by
# exact version; CI installs it one-shot with `npm install --no-save`, so node_modules/ and a
# lockfile are deliberately never committed. Determinism comes from the exact version pins.
node_modules
package-lock.json

# macOS
.DS_Store
